<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>



<%= render partial: 'search_camp' %>
<div id="columns">
  <div id="side">
 
    <% @camps.each do |camp| %>
    
        <ul>
         <bold><%= camp.name %></bold>
             <%#= ": #{camp.address} ; " %>
            <%#= camp.facilities %>
             <%#= "; #{camp.terrain} " %>
             <%#= " -  Fires: #{camp.fires} " %>
            <%#= " -  Primary Activity: #{camp.activities} " %>
          <div><%= link_to "edit", edit_camp_path(camp) %>
          <%= link_to "delete", camp_path(camp), method: :delete, data: { confirm: 'Are   you sure>' }  %>
          <%= link_to "show comments", camp_path(camp) %>
        </div>
        </ul>
    
        <% end %>
      <div>
         <p></p> 
      <%= link_to "Create New Camp", new_camp_path %>
       </div>
  </div>      



  <div id="main">
    <div id="map" style='width: 800px; height: 400px;'></div>
  </div>
</div>

<script type="text/javascript">
handler = Gmaps.build('Google');
handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
  markers = handler.addMarkers(<%=raw @hash.to_json %>);
  handler.bounds.extendWith(markers);
  handler.fitMapToBounds();
});
</script>
